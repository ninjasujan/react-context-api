import _ from "lodash"

const SignUp = () => {
  const colours = ["red", "green", "blue"]
  console.log(_.join(colours))
  return (
    <div>
      <h2>Signup</h2>
    </div>
  )
}

export default SignUp
