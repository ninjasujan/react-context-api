import _ from "lodash"

const Login = () => {
  const colours = ["red", "green", "blue"]
  console.log(_.join(colours))

  return (
    <div>
      <h2>Login</h2>
    </div>
  )
}

export default Login
